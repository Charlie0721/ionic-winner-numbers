import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'winning-numbers',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
